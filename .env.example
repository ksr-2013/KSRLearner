# OpenAI API Configuration (Optional - for premium features)
OPENAI_API_KEY=sk-your-complete-api-key-here
OPENAI_MODEL=gpt-4o

# Google Gemini API Configuration (Free tier available)
GEMINI_API_KEY=your-gemini-api-key-here

# App URL for production (replace with your actual Netlify URL)
NEXT_PUBLIC_APP_URL=https://ksrlearner.netlify.app

# Optional: Set max tokens per response (default: 1000)
MAX_TOKENS=1000

# Optional: Set temperature for creativity (0.0-2.0, default: 0.7)
TEMPERATURE=0.7
